
define(['jquery'],function($){'use strict';const filter=document.querySelector(`.filter_wrap`);return{init:function(){filter.addEventListener('click',function(e){let target=e.target;while(target!=filter){if(target.dataset.handler===`filter_flag`){if(target.classList.contains('filter_active')){$.each($('form input[alt="Flagged"]'),function(key,value){$(value).parents('.que').fadeOut();});target.classList.remove('filter_active');}else{$.each($('form input[alt="Flagged"]'),function(key,value){$(value).parents('.que').fadeIn();});target.classList.add('filter_active');}
return}
if(target.dataset.handler===`filter_answered`){if(target.classList.contains('filter_active')){$.each($('form .answersaved'),function(key,value){$(value).fadeOut();});target.classList.remove('filter_active');}else{$.each($('form .answersaved'),function(key,value){$(value).fadeIn();});target.classList.add('filter_active');}
return}
if(target.dataset.handler===`filter_notanswered`){if(target.classList.contains('filter_active')){$.each($('form .notyetanswered'),function(key,value){$(value).fadeOut();});target.classList.remove('filter_active');}else{$.each($('form .notyetanswered'),function(key,value){$(value).fadeIn();});target.classList.add('filter_active');}}
target=target.parentNode;}});}}});